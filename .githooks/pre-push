#!/usr/bin/env bash
# Antes de subir nada, la tanda completa: tipos, cálculo, fusión, backend,
# interfaz, migración y las dos sincronizaciones. Unos tres minutos.
#
# Para saltárselo puntualmente: git push --no-verify
set -e
cd "$(git rev-parse --show-toplevel)"
npm test
